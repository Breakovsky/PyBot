# Функционал Telegram бота

## 📋 Доступные команды

### `/start`
- Приветственное сообщение
- Сообщения автоматически удаляются через 30 секунд

### `/botexec`
- Показывает статус бота
- Кнопка "Показать время работы" - отображает uptime бота
- Сообщение удаляется через 10 секунд после нажатия на кнопку

## 🔍 Поиск в Excel (Топик ID: 38)

В топике с ID 38 бот автоматически обрабатывает текстовые сообщения как поисковые запросы:

### Типы поиска:

1. **Поиск по ФИО** (частичное совпадение)
   - Просто отправьте имя или фамилию
   - Пример: `Иванов`, `Петр`, `Сергей`

2. **Поиск по телефону или IP-адресу**
   - Отправьте номер телефона или IP-адрес
   - Пример: `192.168.1.1`, `2583109`

3. **Поиск по рабочей станции (WS)**
   - Начните запрос с `ws` (регистр не важен)
   - Пример: `ws111`, `WS222`

### Результаты поиска:
- Отображаются в виде HTML-таблицы
- Поля: ФИО, Подразделение, WorkStation, Телефон, AD, Примечание
- Если результатов много, разбивается на части

## 📡 Мониторинг IP-адресов (Топик ID: 40)

**⚠️ ВНИМАНИЕ:** Функция мониторинга IP-адресов реализована, но **не активна** в текущей версии.

Функция `check_ips_and_notify` из `ping_handler.py` не вызывается в основном коде.

### Что должно работать (когда будет активировано):
- Автоматическая проверка IP-адресов из `ip_addresses.xml`
- Уведомления при изменении статуса (up/down)
- Проверка каждые 60 секунд

## ⚙️ Настройки

### Автоудаление сообщений:
- Сообщения пользователей: 30 секунд
- Сообщения бота: 600 секунд (10 минут)
- Сообщения с кнопками: не удаляются

### Разрешенные топики:
- 38 - Excel поиск
- 40 - Ping мониторинг
- 42 - Команды бота
- 164 - Дополнительный топик

## 🔧 Конфигурация

Все настройки в `main/assets/config.py` (теперь поддерживает `.env` файл):
- `TOKEN` - токен Telegram бота
- `EXCEL_PASSWORD` - пароль для Excel файла
- `EXCEL_FILE_PATH` - путь к Excel файлу
- `IP_ADDRESSES_PATH` - путь к XML с IP-адресами
- `EXCEL_TOPIC_ID`, `PING_TOPIC_ID`, `BOT_TOPIC_ID` - ID топиков

## 📝 Логирование

Логи сохраняются в `bot_log.log` с уровнем DEBUG.

## 🚀 Запуск

```bash
cd TelegramBot/main
python main.py
```

Или как Windows Service:
```bash
python BAS.py install
python BAS.py start
```

