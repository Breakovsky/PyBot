# Веб-интерфейс TBot v2.1

## Описание

Современный веб-интерфейс для управления Telegram ботом, построенный на FastAPI и современном фронтенде.

## Возможности

### ✅ Реализовано

1. **Аутентификация**
   - Вход по email и паролю
   - JWT токены для безопасности
   - Автоматическая проверка авторизации

2. **Дашборд**
   - Статистика по сотрудникам, отделам, рабочим станциям
   - Статистика пользователей Telegram
   - Статистика OTRS (тикеты и действия)

3. **Управление сотрудниками (Список ПК)**
   - Просмотр списка сотрудников
   - Поиск по ФИО, телефону, email
   - Добавление новых сотрудников
   - Редактирование существующих
   - Удаление сотрудников
   - Управление отделами и рабочими станциями

4. **Настройки бота**
   - Просмотр всех настроек
   - Редактирование несекретных настроек
   - Группировка по категориям (Telegram, OTRS, SMTP, Мониторинг)

### 🔜 В разработке

- Полное управление настройками через веб-интерфейс
- Управление ботом (старт/стоп/перезапуск)
- Интеграция с Active Directory
- Интеграция с почтой
- Расширенная статистика и отчёты

## Структура

```
v2_1/
├── main/
│   ├── api/                    # FastAPI приложение
│   │   ├── app.py             # Основное приложение
│   │   ├── dependencies.py    # Зависимости (auth, DB)
│   │   └── v1/                # API v1
│   │       ├── auth.py        # Аутентификация
│   │       ├── dashboard.py   # Дашборд
│   │       ├── employees.py   # Управление сотрудниками
│   │       └── settings.py    # Управление настройками
│   └── web_server.py          # Запуск веб-сервера
└── web/                        # Фронтенд
    ├── templates/              # HTML шаблоны
    │   └── index.html
    └── static/                 # Статические файлы
        ├── css/
        │   └── style.css
        └── js/
            └── app.js
```

## Запуск

### Вместе с ботом (по умолчанию)

Веб-сервер запускается автоматически вместе с ботом:

```powershell
.\bot.ps1 start
```

Веб-интерфейс будет доступен по адресу: `http://localhost:8000`

### Отдельно (только веб-сервер)

```powershell
python main\web_server.py
```

### Настройка

В `.env` файле можно настроить:

```env
WEB_ENABLED=true          # Включить/выключить веб-сервер (по умолчанию: true)
WEB_HOST=0.0.0.0         # Хост для прослушивания (по умолчанию: 0.0.0.0)
WEB_PORT=8000             # Порт (по умолчанию: 8000)
WEB_RELOAD=false          # Автоперезагрузка при изменении кода (для разработки)
```

## API Endpoints

### Аутентификация

- `POST /api/v1/auth/login` - Вход в систему
- `GET /api/v1/auth/me` - Информация о текущем пользователе
- `POST /api/v1/auth/logout` - Выход

### Дашборд

- `GET /api/v1/dashboard/stats` - Статистика

### Сотрудники

- `GET /api/v1/employees` - Список сотрудников (с пагинацией и поиском)
- `GET /api/v1/employees/{id}` - Информация о сотруднике
- `POST /api/v1/employees` - Создать сотрудника
- `PUT /api/v1/employees/{id}` - Обновить сотрудника
- `DELETE /api/v1/employees/{id}` - Удалить сотрудника
- `GET /api/v1/employees/departments/list` - Список отделов
- `GET /api/v1/employees/workstations/list` - Список рабочих станций

### Настройки

- `GET /api/v1/settings` - Все настройки (с фильтром по категории)
- `GET /api/v1/settings/{key}` - Конкретная настройка
- `PUT /api/v1/settings/{key}` - Обновить настройку
- `GET /api/v1/settings/categories/list` - Список категорий

## Безопасность

- Все API endpoints (кроме `/api/v1/auth/login`) требуют JWT токен
- Токен передаётся в заголовке `Authorization: Bearer <token>`
- Токен действителен 24 часа
- Секретные настройки не могут быть изменены через API
- Только администраторы могут управлять настройками и сотрудниками

## Создание первого пользователя

Для входа в веб-интерфейс нужно создать пользователя в БД:

```sql
-- Создать пользователя-администратора
INSERT INTO core.users (email, password_hash, full_name, is_active, is_admin)
VALUES (
    'admin@yourcompany.com',
    '$2b$12$...',  -- bcrypt hash пароля
    'Администратор',
    TRUE,
    TRUE
);
```

Или используйте скрипт для создания пользователя (будет создан позже).

## Документация API

После запуска веб-сервера доступна автоматическая документация:

- Swagger UI: `http://localhost:8000/api/docs`
- ReDoc: `http://localhost:8000/api/redoc`

## Разработка

Для разработки с автоперезагрузкой:

```powershell
$env:WEB_RELOAD="true"
python main\web_server.py
```

Или в `.env`:

```env
WEB_RELOAD=true
```

## Следующие шаги

1. Создать скрипт для создания первого пользователя
2. Реализовать полное управление настройками через веб-интерфейс
3. Добавить управление ботом (старт/стоп/перезапуск)
4. Расширить функционал дашборда
5. Добавить интеграцию с AD и почтой

