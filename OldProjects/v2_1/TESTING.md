# Тестирование TBot v2.1

## Быстрый запуск тестов

### Вариант 1: Через bat-файл (Windows)

```powershell
.\run_tests.bat
```

### Вариант 2: Через Python

```powershell
python test_bot.py
```

## Что проверяют тесты

1. **Подключение к БД** - проверяет соединение с PostgreSQL
2. **Инициализация настроек** - проверяет создание таблицы настроек
3. **Репозиторий сотрудников** - проверяет работу с данными сотрудников
4. **Security Manager** - проверяет доступ к Windows Credential Manager
5. **Обработчики** - проверяет создание всех обработчиков бота

## Требования перед запуском тестов

1. ✅ PostgreSQL должен быть запущен
2. ✅ База данных `tbot` должна быть создана
3. ✅ Миграции должны быть применены (`alembic upgrade head`)
4. ✅ Секреты должны быть сохранены в Windows Credential Manager:
   - `TOKEN` - токен Telegram бота
   - `SUPERCHAT_TOKEN` - ID чата
   - `DB_PASSWORD` - пароль БД
5. ✅ Файл `.env` должен быть создан с настройками БД

## Интерпретация результатов

### ✅ Все тесты пройдены
```
🎉 Все тесты пройдены! Бот готов к запуску.
```
Бот готов к запуску. Можно запускать `python main/main.py` или `.\run_bot.bat`

### ❌ Некоторые тесты не пройдены

**Если не пройден тест "Подключение к БД":**
- Проверьте, что PostgreSQL запущен
- Проверьте настройки в `.env` (DB_HOST, DB_PORT, DB_NAME, DB_USER)
- Проверьте пароль в Windows Credential Manager
- Проверьте права пользователя БД

**Если не пройден тест "Инициализация настроек":**
- Убедитесь, что миграции применены: `alembic upgrade head`
- Проверьте, что таблица `core.settings` существует

**Если не пройден тест "Security Manager":**
- Убедитесь, что секреты сохранены в Windows Credential Manager
- Используйте команды из `QUICK_START.md` для сохранения секретов

**Если не пройден тест "Репозиторий сотрудников":**
- Проверьте, что схема `employees` создана
- Проверьте, что таблица `employees.employees` существует
- Убедитесь, что миграции применены

## Ручное тестирование функций бота

После успешного прохождения тестов:

1. **Запустите бота:**
   ```powershell
   python main/main.py
   # или
   .\run_bot.bat
   ```

2. **Тест авторизации:**
   - Откройте Telegram, найдите бота
   - Отправьте `/start` в личные сообщения
   - Нажмите "Авторизоваться"
   - Введите корпоративный email
   - Проверьте почту и введите код

3. **Тест команд:**
   - `/status` - статус авторизации
   - `/mystats` - статистика OTRS
   - `/botexec` - время работы бота

4. **Тест поиска сотрудников:**
   - Откройте групповой чат с ботом
   - Перейдите в топик с ID из `EXCEL_TOPIC_ID`
   - Отправьте имя сотрудника или IP адрес
   - Проверьте ответ бота

## Отладка

Если что-то не работает:

1. **Проверьте логи:**
   ```powershell
   type logs\tbot.log
   ```

2. **Проверьте подключение к БД вручную:**
   ```powershell
   psql -U tbot -d tbot -h localhost
   ```

3. **Проверьте секреты:**
   ```powershell
   python -c "import keyring; print(keyring.get_password('TBot', 'TOKEN'))"
   ```

4. **Проверьте переменные окружения:**
   ```powershell
   python -c "import os; from dotenv import load_dotenv; load_dotenv(); print('DB_HOST:', os.getenv('DB_HOST'))"
   ```

