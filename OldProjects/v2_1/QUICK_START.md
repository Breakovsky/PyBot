# Быстрый старт TBot v2.1

## Шаг 1: Установка зависимостей

```powershell
cd v2_1
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## Шаг 2: Настройка PostgreSQL

1. Убедитесь, что PostgreSQL запущен
2. Создайте базу данных (если еще не создана):

```sql
CREATE DATABASE tbot;
CREATE USER tbot WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE tbot TO tbot;
```

## Шаг 3: Сохранение секретов в Windows Credential Manager

```powershell
# Токен Telegram бота
python -c "import keyring; keyring.set_password('TBot', 'TOKEN', 'YOUR_BOT_TOKEN')"

# ID чата (отрицательное число для групп)
python -c "import keyring; keyring.set_password('TBot', 'SUPERCHAT_TOKEN', '-1001234567890')"

# Пароль БД
python -c "import keyring; keyring.set_password('TBot', 'DB_PASSWORD', 'your_secure_password')"
```

## Шаг 4: Создание .env файла

Создайте файл `.env` в корне проекта `v2_1/`:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tbot
DB_USER=tbot

# Telegram Topics (замените на ваши ID)
EXCEL_TOPIC_ID=38
PING_TOPIC_ID=40
BOT_TOPIC_ID=42
METRICS_TOPIC_ID=0
TASKS_TOPIC_ID=145

# Cluster (опционально)
CLUSTER_ENABLED=false

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/tbot.log
```

## Шаг 5: Применение миграций БД

```powershell
cd v2_1
alembic upgrade head
```

Если миграции еще не созданы, создайте их:

```powershell
alembic revision --autogenerate -m "Initial schema"
alembic upgrade head
```

## Шаг 6: Тестирование

Запустите тестовый скрипт для проверки конфигурации:

```powershell
python test_bot.py
```

Ожидаемый вывод:
```
============================================================
ТЕСТИРОВАНИЕ TBot v2.1
============================================================

============================================================
Тест 1: Подключение к базе данных
============================================================
✅ Подключение к БД успешно

============================================================
Тест 2: Инициализация настроек
============================================================
✅ Настройки инициализированы

...

🎉 Все тесты пройдены! Бот готов к запуску.
```

## Шаг 7: Запуск бота

```powershell
python main/main.py
```

## Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start` в личные сообщения
3. Попробуйте авторизоваться через email
4. Проверьте команды: `/status`, `/mystats`, `/botexec`

## Troubleshooting

### Ошибка: "TOKEN not found"
- Убедитесь, что токен сохранен в Windows Credential Manager
- Проверьте правильность команды сохранения

### Ошибка: "Database connection failed"
- Проверьте, что PostgreSQL запущен
- Проверьте правильность пароля в Credential Manager
- Проверьте настройки в .env файле

### Ошибка: "No module named 'asyncpg'"
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r requirements.txt`

### Ошибка миграций
- Убедитесь, что база данных создана
- Проверьте права пользователя БД
- Попробуйте создать миграцию заново: `alembic revision --autogenerate -m "Initial"`

## Следующие шаги

После успешного запуска:
1. Импортируйте данные сотрудников (если есть Excel файл)
2. Настройте мониторинг серверов
3. Настройте интеграцию с OTRS
4. Включите кластеризацию (если нужно)

