# Исправления проблем

## ✅ Проблема 1: Конфликт множественных экземпляров бота

**Симптомы:**
```
TelegramConflictError: Conflict: terminated by other getUpdates request; 
make sure that only one bot instance is running
```

**Причина:** Запущено несколько экземпляров бота одновременно.

**Решение:**
1. ✅ Улучшен `bot.ps1` - теперь автоматически находит и останавливает все старые процессы перед запуском
2. ✅ Добавлена проверка единственного экземпляра в `main.py` (через lock файл)
3. ✅ Добавлен `psutil` в `requirements.txt` для проверки процессов

**Использование:**
```powershell
# Остановить все процессы и запустить заново
.\bot.ps1 restart

# Проверить статус
.\bot.ps1 status
```

## ✅ Проблема 2: Неправильное имя переменной для ID чата

**Симптомы:**
```
Failed to send startup message: Telegram server says - Bad Request: chat not found
```

**Причина:** Использовалась переменная `SUPERCHAT_TOKEN` вместо `TELEGRAM_CHAT_ID`.

**Решение:**
1. ✅ Заменено `SUPERCHAT_TOKEN` на `TELEGRAM_CHAT_ID` в коде
2. ✅ Обновлен скрипт `init_settings.py` для добавления `TELEGRAM_CHAT_ID`

**Настройка:**
Добавьте в `.env`:
```env
TELEGRAM_CHAT_ID=-1001234567890  # ID вашего группового чата (отрицательное число)
```

Или инициализируйте через скрипт:
```powershell
.\bot.ps1 init-settings
```

## 📋 Текущие настройки TOPIC_ID

Все настройки сохранены в БД:
- `TELEGRAM_CHAT_ID` - ID группового чата
- `EXCEL_TOPIC_ID` = 9
- `PING_TOPIC_ID` = 7
- `BOT_TOPIC_ID` = 5
- `METRICS_TOPIC_ID` = 69
- `TASKS_TOPIC_ID` = 145

## 🔧 Команды для управления

```powershell
# Просмотр настроек
.\bot.ps1 settings

# Инициализация/обновление настроек
.\bot.ps1 init-settings

# Запуск бота
.\bot.ps1 start

# Проверка статуса
.\bot.ps1 status

# Просмотр логов
.\bot.ps1 logs

# Остановка
.\bot.ps1 stop
```

## ✅ Текущий статус

- ✅ Бот запущен без конфликтов
- ✅ Настройки TOPIC_ID инициализированы в БД
- ✅ Улучшена обработка множественных экземпляров
- ✅ Исправлена проблема с отправкой startup message

## ⚠️ Что нужно сделать

1. **Добавить TELEGRAM_CHAT_ID в .env:**
   ```env
   TELEGRAM_CHAT_ID=-1001234567890
   ```

2. **Инициализировать настройки:**
   ```powershell
   .\bot.ps1 init-settings
   ```

3. **Перезапустить бота:**
   ```powershell
   .\bot.ps1 restart
   ```

