# Настройка TOPIC_ID и других параметров

## Где хранятся настройки

Настройки топиков и другие параметры могут храниться в двух местах:

1. **База данных** (`core.settings`) - приоритетный источник
2. **Переменные окружения** (`.env` файл) - резервный источник

## Быстрая настройка

### Вариант 1: Через скрипт (рекомендуется)

```powershell
# Инициализировать настройки в БД из .env или значений по умолчанию
.\bot.ps1 init-settings

# Посмотреть текущие настройки
.\bot.ps1 settings
```

### Вариант 2: Через .env файл

Создайте файл `.env` в корне проекта `v2_1/`:

```env
# Telegram Topics
EXCEL_TOPIC_ID=38
PING_TOPIC_ID=40
BOT_TOPIC_ID=42
METRICS_TOPIC_ID=0
TASKS_TOPIC_ID=145

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tbot
DB_USER=tbot

# Other
BOT_STARTUP_MESSAGE=🤖 Бот включился и готов к работе!
MONITOR_CHECK_INTERVAL=30
OTRS_CHECK_INTERVAL=60
CLUSTER_ENABLED=false
```

Затем запустите:
```powershell
.\bot.ps1 init-settings
```

### Вариант 3: Напрямую в БД

```python
python scripts\init_settings.py
```

## Необходимые TOPIC_ID

Для работы бота нужны следующие ID топиков:

| Настройка | Описание | Пример |
|-----------|----------|--------|
| `EXCEL_TOPIC_ID` | Топик для поиска сотрудников | `38` |
| `PING_TOPIC_ID` | Топик для мониторинга серверов | `40` |
| `BOT_TOPIC_ID` | Топик для сообщений бота | `42` |
| `METRICS_TOPIC_ID` | Топик для метрик (опционально) | `0` |
| `TASKS_TOPIC_ID` | Топик для заявок OTRS | `145` |

## Как узнать ID топика

1. Откройте групповой чат в Telegram
2. Откройте нужный топик
3. Скопируйте ссылку на топик (если доступна)
4. Или используйте бота @userinfobot - он покажет `message_thread_id` при отправке сообщения в топик

## Проверка настроек

```powershell
# Показать все настройки из БД
.\bot.ps1 settings

# Или через Python
python scripts\show_settings.py
```

## Обновление настроек

### Через скрипт

Измените `.env` файл и запустите:
```powershell
.\bot.ps1 init-settings
```

### Через Python (программно)

```python
from main.config.settings import get_settings

settings = get_settings()
await settings.set("EXCEL_TOPIC_ID", "38", category="telegram", description="ID топика для поиска сотрудников")
```

## Приоритет загрузки

1. **База данных** (`core.settings`) - используется в первую очередь
2. **Переменные окружения** (`.env`) - используется если настройки нет в БД
3. **Значения по умолчанию** - используются если ничего не найдено

## Важно

- После изменения настроек **перезапустите бота**: `.\bot.ps1 restart`
- Настройки кэшируются на 5 минут
- Секреты (TOKEN, DB_PASSWORD) хранятся в Windows Credential Manager, не в БД

## Troubleshooting

### Бот не обрабатывает сообщения в топике

1. Проверьте, что TOPIC_ID установлен правильно:
   ```powershell
   .\bot.ps1 settings
   ```

2. Убедитесь, что ID топика правильный (можно проверить через @userinfobot)

3. Проверьте, что бот добавлен в группу и имеет права на чтение сообщений

4. Перезапустите бота:
   ```powershell
   .\bot.ps1 restart
   ```

### Настройки не применяются

1. Убедитесь, что настройки сохранены в БД:
   ```powershell
   .\bot.ps1 settings
   ```

2. Проверьте логи:
   ```powershell
   .\bot.ps1 logs
   ```

3. Перезапустите бота для применения изменений

